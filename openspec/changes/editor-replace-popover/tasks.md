## 1. Verify implementation matches spec

- [ ] 1.1 Walk `ReplacePopover` against Requirement: Shape band and Icons band (sections, stencil keys, primitives, search + cap).
- [ ] 1.2 Confirm `replaceElement` and `ReplaceTemplate` in editor store satisfy Requirement: Replace preserves layout styling and stencil regeneration rules.

## 2. Close change in OpenSpec

- [ ] 2.1 When product behavior intentionally diverges from this spec, update `spec.md` delta before archiving.
- [ ] 2.2 Run `/opsx:archive` (or `openspec archive`) when this capability is finalized so `openspec/specs/` carries the merged truth.
